// Tailwind CSS v4 — single PostCSS plugin. No autoprefixer (v4 vendors its own
// via Lightning CSS), no tailwind.config.js (v4 is CSS-first).
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
